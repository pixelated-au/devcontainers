.DS_Store
node_modules/

# Per-machine Claude Code permission grants. Must never be committed: anything
# under src/<template>/ is copied verbatim into every user's project.
.claude/settings.local.json
