{
    "id": "claude-sandbox",
    "version": "1.8.0",
    "name": "Claude Sandbox",
    "description": "Runs Claude Code inside a Docker sandbox with a default-deny outbound firewall. This isn't a general-purpose development container; it exists to give Claude a contained environment where it can only reach an explicit allow-list of hosts.",
    "documentationURL": "https://github.com/pixelated-au/devcontainers/tree/main/src/claude-sandbox",
    "licenseURL": "https://github.com/pixelated-au/devcontainers/blob/main/LICENSE",
    "publisher": "Pixelated",
    "keywords": [
        "claude",
        "claude-code",
        "sandbox",
        "firewall",
        "node",
        "php"
    ],
    "options": {
        "timezone": {
            "type": "string",
            "description": "IANA timezone for the container. The host's $TZ takes precedence when it is set.",
            "proposals": [
                "Australia/Melbourne",
                "Australia/Sydney",
                "Australia/Brisbane",
                "Australia/Perth",
                "Pacific/Auckland",
                "UTC",
                "Europe/London",
                "America/New_York",
                "America/Los_Angeles"
            ],
            "default": "Australia/Melbourne"
        },
        "phpVersion": {
            "type": "string",
            "description": "PHP version to install. The feature compiles from source, so a version it cannot fetch fails the image build rather than falling back. 'latest' tracks whatever upstream calls current at build time.",
            "proposals": [
                "latest",
                "8.4",
                "8.3",
                "8.2"
            ],
            "default": "latest"
        },
        "workspaceFolder": {
            "type": "string",
            "description": "Folder name to mount this project under /workspaces inside the container. Claude keys its session history by path, so projects sharing a name share their history in the common config volume. The default resolves to the name of your project folder on the host.",
            "proposals": [
                "${localWorkspaceFolderBasename}"
            ],
            "default": "${localWorkspaceFolderBasename}"
        }
    },
    "platforms": [
        "Any"
    ]
}
